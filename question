尝试过静态的注意力机制感觉不再算注意力机制了
具体研究切入点应该在哪呢，之前RRAM实现的加速都是缓变的，对于这种快变的相关工作少一些。有没有之前RRAM加速的参考
经过softmax以后为高度稀疏的矩阵，对这种矩阵处理上有什么办法，或参考经验？保留千级的数据剩下的直接置零也可以实现操作
时变X时变的数据，降低数据量，设计一种采样方法，不再进行全局采样？但实际思考发现，RRAM的高度并行性下，数据量并不是特别大的问题，FC这种反而更适合RRAM进行
异构的思路的话，数据流和仿真问题，如何进行数模架构的混合仿真
embedding操作效果明显
算法上有没有降到线性复杂度的方法。
器件层次难度较大，这方面的思考比较少。
能不能使用一些近似手段，把他尽量放在模拟计算部分进行
